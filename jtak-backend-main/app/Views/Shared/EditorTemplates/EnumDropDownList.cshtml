@using Solf.Extensions
@using System.Linq
@model Enum
@{
    var enumType = Nullable.GetUnderlyingType(ViewData.ModelMetadata.ModelType) ?? ViewData.ModelMetadata.ModelType;
    
    var values = Enum.GetValues(enumType)
                     .Cast<Enum>()
                     .Select(v => new SelectListItem
                     {
                         Selected = Model == v,
                         Text = v.ToLocalizedName(),
                         Value = v.ToString()
                     });
}
<div class="form-group">
    <label asp-for="@Model" class="control-label"></label>
    <select asp-for="@Model" asp-items="@values" class="@ViewData["class"]">
        @if (ViewBag.placeholder != null)
        {
            <option value="">@ViewBag.placeholder</option>
        }
    </select>
    <div class="text-danger validationbox">
        <span asp-validation-for="@Model" class="text-danger"></span>
    </div>
</div>