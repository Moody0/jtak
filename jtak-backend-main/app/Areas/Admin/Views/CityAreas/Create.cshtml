@*@using Solf.Extensions
@using App.Resources
@model App.Entities.CityAreaDto
@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer Localizer

<div class="container-fluid">
    <form asp-action="Create">
        <div class="card card-default">
            <div class="card-header">
                <em class="fa fa-box p-2"></em> @Localizer["Create CityArea."]
            </div>
            <div class="card-body">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="row">
                    <div class="col-md-6">
                        @Html.EditorFor(x => x.TitleAr)
                        @Html.EditorFor(x => x.TitleEn)
                        @Html.EditorFor(x => x.TitleTr)
                        @Html.EditorFor(x => x.CityId)
                    </div>
                    <div class="col-md-6">
                    </div>
                </div>
            </div>
            <div class="card-footer text-right">
                <div class="btn-group">
                    <a asp-action="Index" class="btn btn-secondary">@_Common.BackToList</a>
                    <input type="submit" value="@_Common.Create" class="btn btn-success" />
                </div>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}*@